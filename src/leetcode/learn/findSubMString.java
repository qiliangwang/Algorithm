package leetcode.learn;

public class findSubMString {

    public int finString(String n, char[] m) {
        int nL = n.length();
        int mL = m.length;
        for (int i = 0; i < nL; i ++ ) {
            //
        }
        return -1;
    }
}
